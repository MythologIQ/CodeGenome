[package]
name = "codegenome-cli"
version = "0.1.0"
edition = "2021"
description = "CLI for the Unified Code Reality Graph"

[[bin]]
name = "codegenome"
path = "src/main.rs"

[dependencies]
codegenome-core = { path = "../codegenome-core" }
clap = { version = "4", features = ["derive"] }
